import promise, { request, requestAxios, sleep } from "./promise";
import storage from "./storage";
import { logger, getPrefixedLogger } from "./extendedLogger";

export default { promise, request, requestAxios, storage, sleep, logger, getPrefixedLogger };
